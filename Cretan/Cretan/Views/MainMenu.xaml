<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Cretan.Views.MainMenu" Title="Cretan">
    <ContentPage.Content>
        <Grid VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="25"/>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Button HorizontalOptions="FillAndExpand" Command="{Binding Navigation}" 
                    CommandParameter="MainMenu\FreeRunSetup"  Text="Free Run"  Font="Bold, 28"  HeightRequest="70" Grid.Row="0" />
            <Button HorizontalOptions="FillAndExpand" Command="{Binding Navigation}" 
                    CommandParameter="MainMenu\CretanPath" Text="Cretan Path" Font="Bold, 28" HeightRequest="70"  Grid.Row="2"/>
            <Button HorizontalOptions="FillAndExpand" Command="{Binding Navigation}" 
                    CommandParameter="MainMenu\Help" Text="Help" Font="Bold, 20" HeightRequest="70" Grid.Row="4"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>